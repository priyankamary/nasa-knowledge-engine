# Core RAG stack
langchain>=0.2.11
langchain-community>=0.2.10
langchain-huggingface>=0.0.3
chromadb>=0.5.5
pypdf>=4.2.0
sentence-transformers>=3.0.0

# HF inference + quantization
transformers>=4.42.0
accelerate>=0.33.0
bitsandbytes>=0.43.0      # optional; requires CUDA. Safe to install; it no-ops on CPU.

# LLM runner deps
torch>=2.3.0               # if you deploy on GPU boxes, match the CUDA build you need
torchvision>=0.18.0      # uncomment only if you need it

# API
flask>=3.0.0
flask-cors>=4.0.0
pyngrok>=7.2.1             # optional, for tunneling

# Optional utils
gTTS>=2.5.0

# Version pins to avoid CLI conflicts
click==8.1.7
typer==0.12.3
gradio==4.44.1             # optional; only if you add a quick UI later
